const Topbar = () => {
    return (
      <header className="topbar">
        <div>Dashboard</div>
        <button className="workflow-btn">+ Create New Workflow</button>
      </header>
    );
  };
  
  export default Topbar;
  